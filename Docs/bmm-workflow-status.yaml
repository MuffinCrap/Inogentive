# Workflow Status Template
# This tracks progress through BMM methodology phases
# Phase 3/4 (Implementation) is tracked separately in sprint-status.yaml

generated: "2025-11-11"
project: "Weekly Report Analysis"
project_type: "software"
selected_track: "enterprise"
field_type: "brownfield"
workflow_path: "enterprise-brownfield.yaml"

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

workflow_status:
  # Prerequisite: Documentation (brownfield system needs documenting)
  document-project: Docs/index.md  # agent: analyst

  # Phase 0: Discovery
  brainstorm-project: required  # agent: analyst
  research: required  # agent: analyst

  # Phase 1: Planning
  prd: required  # agent: pm
  validate-prd: recommended  # agent: pm
  create-ux-design: recommended  # agent: ux-designer
  framework: optional  # agent: tea
  ci: optional  # agent: tea

  # Phase 2: Solutioning
  architecture: required  # agent: architect
  security-architecture: optional  # agent: architect
  devops-strategy: optional  # agent: architect
  validate-architecture: optional  # agent: architect
  solutioning-gate-check: required  # agent: architect

  # Phase 3: Implementation
  sprint-planning: required  # agent: sm
  dev-story: required  # agent: sm
